//! Facade for the unified poker hand evaluator.
//!
//! The full `HandRule` trait, the feature-gated re-exports of every variant
//! crate, and the `HiLoRule` composite are introduced in step 7. This file
//! is intentionally minimal during bootstrap so the workspace builds.

#![cfg_attr(docsrs, feature(doc_cfg))]
